<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Dark/AMOLED system theme: start on the deep gradient stop so the splash
      blends into the dark app instead of flashing bright teal.
    -->
    <style name="Theme.TransitOS.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.TransitOS</item>
    </style>
</resources>
